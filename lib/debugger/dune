(library
 (name butter_debugger)
 (libraries menhirLib butter_isa common butter_sim)
 (modules ast parser lexer debugger))

(menhir
 (modules parser)
 ; (flags --exn-carries-state --trace --dump)
 ; (flags --exn-carries-state)
 ; (flags --strict)
 )

(ocamllex
 (modules lexer))
